# HTTP経由(ブラウザ経由)でのサーバーの設定変更を許可するユーザーを設定します 
# "#"で始まる行はコメントとして無視されます 
# allow の後の正規表現にマッチするIPに、難度やマップなどのゲームの設定を変更する権限を与えます (例: allow 192\.168\..*) 
# admin の後の正規表現にマッチするIPには、ゲームの設定に加え、最大人数、ポーズの許可などのサーバーの設定を変更する権限を与えます (例: admin 127\.0\.0\.1) 
# "allow players" "admin players" の記述は特別で、これがある場合今ゲームに参加しているプレイヤーのIPに権限を与えます 
# deny の後の正規表現にマッチするIPは、allowやadminの条件に適合しても拒否します (例: deny 192\.168\.0\.(4|13)) 
# このファイルはゲーム中サーバーモードにする度に再読み込みされます 
# このファイルが存在しない場合、ローカルのみに管理権限を与える設定 (admin 127\.0\.0\.1 と同一) になります 
# 正規表現の記述が間違ってる場合、stderr.txtに該当行を書き出します 

#allow players
admin 192\.168\..*
admin 127\.0\.0\.1
